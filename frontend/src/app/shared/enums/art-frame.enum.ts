export type ArtFrame = 'default' | 'classic' | 'modern' | 'photo';

export const ArtFrame = {
    DEFAULT: 'default' as ArtFrame,
    CLASSIC: 'classic' as ArtFrame,
    MODERN: 'modern' as ArtFrame,
    PHOTO: 'photo' as ArtFrame,
}