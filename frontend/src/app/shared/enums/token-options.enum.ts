export type TokenOptions = 'session_id' | 'session_expiration';

export const TokenOptions = {
    session_id: 'session_id' as TokenOptions,
    session_expiration: 'session_expiration' as TokenOptions
}